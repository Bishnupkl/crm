$(".menu-toggler.sidebar-toggler").on('click',function(event){
  $(".nav-link").toggleClass("position-nav-link");
  // when the burger button is pressed, it'll change the padding of the .navlink so that the icons will be in position
});
